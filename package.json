{
  "name": "restructure-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Validation and test tooling for the Restructure skill and optional MCP review plugin.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-plugin.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
